# Documentation site toolchain (plan 16 phase 5). Not needed to run DeviceKit — only to
# build the docs site:  pip install -r docs/requirements.txt  &&  mkdocs serve
mkdocs>=1.6
mkdocs-material>=9.5
